%h1 Listing tracks

%table
  %thead
    %tr
      %th Track name
      %th Track description
      %th Track image
      %th Track audio
      %th Track metering levels
      %th
      %th
      %th

  %tbody
    - @tracks.each do |track|
      %tr
        %td= track.track_name
        %td= track.track_description
        %td= track.track_image
        %td= track.track_audio
        %td= track.track_metering_levels
        %td= link_to 'Show', track
        %td= link_to 'Edit', edit_track_path(track)
        %td= link_to 'Destroy', track, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Track', new_track_path
